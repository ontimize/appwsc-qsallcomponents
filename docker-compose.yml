version: '3'

services:
   appwsc-qsallcomponent-jee-reports-db:
      container_name: appwsc-qsallcomponent-jee-reports-db
      image: appwsc-qsallcomponent-jee-reports-db
         
      networks:
         - net    
   appwsc-qsallcomponent-jee-reports-server:
      container_name: appwsc-qsallcomponent-jee-reports-boot
      image: appwsc-qsallcomponent-jee-reports-boot
      ports:
         - "9092:8080"
      depends_on:
         - appwsc-qsallcomponent-jee-reports-db
      networks:
         - net
      volumes:
        - "/opt/docker-data/appwsc-qsallcomponents-reports/logs:/usr/local/spring-boot/logs"

networks:
   net: